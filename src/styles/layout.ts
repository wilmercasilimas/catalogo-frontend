// Espaciado general de sección
export const sectionSpacing = "px-4 py-6 sm:px-6 lg:px-8";

// Contenedor centrado con max-width
export const containerBase = "mx-auto w-full max-w-7xl";

// Espaciado interno entre bloques o tarjetas
export const blockSpacing = "space-y-4 sm:space-y-6";

// Layout base para secciones de contenido
export const sectionBase = `${sectionSpacing} ${containerBase} ${blockSpacing}`;

// Layout de columnas responsivo
export const gridResponsive = "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4";

// Espaciado entre botones o acciones
export const actionRow = "flex flex-wrap items-center justify-between gap-4";

// Espaciado superior fijo para compensar navbar (si usas topbar fija)
export const offsetTopbar = "pt-16 sm:pt-20";

// Espaciado para dashboards
export const dashboardSection = "mt-6 sm:mt-8";
